import styled from "styled-components";

export const Wrapper = styled.div`
  position: absolute;
  color: #fff;
  text-align: center;
  left: 50%;
  transform: translate(-50%);
  font-size: 16px;
`;
